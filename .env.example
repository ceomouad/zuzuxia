# Admin panel password (change this!)
ADMIN_PASSWORD=zuzuxia-admin

# Contact channels
# WhatsApp is the primary order / customer-service channel. Digits only:
# country code + number, no "+" or spaces (e.g. 8613800138000). Leave blank to
# fall back to Instagram for ordering.
NEXT_PUBLIC_WHATSAPP_NUMBER=
# Instagram username (social link + order fallback)
NEXT_PUBLIC_INSTAGRAM_USERNAME=abderrv.hmvne
# TikTok username, without the "@" (leave blank to hide the TikTok link)
NEXT_PUBLIC_TIKTOK_USERNAME=

# ---- Optional: Supabase (leave blank to use the built-in JSON file store) ----
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
